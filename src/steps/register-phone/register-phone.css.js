import { css } from "lit";

export default css`

    .register-phone-container {
        padding: 2rem;
        possition: relative;
        height: 85vh;

        button-default {
            --button-default-margin-top: 1.5rem;
        }
    }

    .register-phone-header{
        display: flex;
        flex-direction: column;
        align-items: center;
        margin-bottom: 2rem;
        gap: 0.5rem;
    }
`;
